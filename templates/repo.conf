{{repo.name}}: {
    url: "{{repo.scheme}}://{{repo.host}}/{{repo.path}}/{{repo.repo}}",
    mirror_type: "{{repo.mirror_type}}", 
    signiture_type: "{{repo.sig_type}}",
    pubkey: "/etc/ssl/{{repo.name}}.cert",
    enabled: yes
}

